# Canonical documentation

This repository’s primary documentation lives in **`README.md`** (uppercase).

That file describes the Bitcoin Blockchain Explorer stack (Go/Gin backend, Redis, OpenAPI **`/api/v1`**, machine API keys, and how to build and run the app).
