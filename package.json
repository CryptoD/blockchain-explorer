{
  "name": "blockchain-explorer-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "chart.js": "^4.4.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.58.2",
    "autoprefixer": "^10.4.14",
    "esbuild": "^0.25.5",
    "playwright": "^1.58.2",
    "postcss": "^8.4.24",
    "sass": "^1.72.1",
    "tailwindcss": "^3.4.7"
  },
  "scripts": {
    "build:css": "mkdir -p dist .tmp && sass src/styles/theme.scss .tmp/theme.css --no-source-map && npx tailwindcss -i .tmp/theme.css -o dist/styles.css --minify",
    "build:charts": "node scripts/build-charts.mjs",
    "analyze:charts": "CHART_BUNDLE_META=1 node scripts/build-charts.mjs",
    "stamp:assets": "node scripts/stamp-frontend-assets.cjs",
    "build": "npm run build:css && npm run build:charts && npm run stamp:assets",
    "watch:css": "mkdir -p dist .tmp && sass --watch src/styles/theme.scss:.tmp/theme.css & npx tailwindcss -i .tmp/theme.css -o dist/styles.css --watch",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:lighthouse": "lhci autorun"
  }
}
